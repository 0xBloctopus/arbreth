[package]
name = "validation"
version = "0.1.0"
edition = "2021"
license = "BUSL-1.1"

[dependencies]
arbutil = { path = "../arbutil" }
brotli = { package = "nitro-brotli", path = "../brotli" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_with = { version = "3.8.1", features = ["base64"] }
