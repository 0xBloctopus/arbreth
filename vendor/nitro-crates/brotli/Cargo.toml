[package]
name = "nitro-brotli"
version = "0.1.0"
edition = "2021"
license = "BUSL-1.1"

[dependencies]
lazy_static = "1"
num_enum = "0.7.2"

[build-dependencies]
cc = "1"

[lib]
crate-type = ["lib"]

[features]
default = ["cc_brotli"]
cc_brotli = []
